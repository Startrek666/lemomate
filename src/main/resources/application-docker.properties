# 数据库配置
spring.datasource.url=jdbc:mysql://mysql:3306/lemomate?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# 服务器配置
server.port=8085
server.servlet.context-path=/api

# JWT配置
jwt.secret=EldqqV5j3JQax6yaKe3XKKNVQLZRpT2g
jwt.expiration=86400000

# Jitsi配置
jitsi.app.id=lemomate_app
jitsi.domain=meet.lemomate.com

# 文件上传配置
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB

# 文件存储路径
file.upload-dir=/app/uploads/avatars